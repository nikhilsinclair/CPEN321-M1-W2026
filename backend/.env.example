# Server configuration
PORT=3000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/cpen321

# Google Auth 
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com

# JWT 
JWT_SECRET=your_jwt_secret_key_here

# Other API keys, secrets, and configurations

# Course pixel stream (TLS validation stays enabled).
PIXEL_SOURCE_URL=wss://8.229.22.124
# Optional PEM certificate file if the course source uses a self-signed certificate.
# PIXEL_SOURCE_CA_FILE=/absolute/path/to/course-certificate.pem

# Football-data.org free API token. Keep the real token only in backend/.env.
FOOTBALL_DATA_API_TOKEN=
